# Phase 57 — Story Bundle Vault

Goal:
- add browser-local vault storage for story bundles
- archive current generated bundles and imported bundles
- restore archived bundle snapshots safely into account-scoped persistence
- keep live XR runtime untouched

Targets:
- vault manifest types
- account-scoped vault storage + Zustand store
- vault bootstrap + panel
- build green
