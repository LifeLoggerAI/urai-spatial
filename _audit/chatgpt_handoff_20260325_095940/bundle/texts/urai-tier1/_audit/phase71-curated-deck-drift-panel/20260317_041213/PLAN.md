# Phase 71 — Curated Deck Drift Panel

Goal:
- derive a browser-local short-window drift view for the active curated deck vault entry
- reuse local curated deck diffs and keep phases 65-70 additive
- expose whether the active entry sits inside a calm or shifting recent trajectory

Targets:
- drift summary types
- pure drift builder
- drift panel mounted in scene shell
- build green
