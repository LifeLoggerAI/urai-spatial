# Phase 81 — Curated Deck Schedule Panel

Goal:
- derive a browser-local processing schedule for the active curated deck vault entry
- convert queue + dispatch + review ticket into cadence, ETA window, and checkpoints
- keep phases 65-80 additive and runtime-safe

Targets:
- schedule summary types
- pure schedule builder
- schedule panel mounted in scene shell
- build green
