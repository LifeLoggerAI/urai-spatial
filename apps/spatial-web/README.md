# spatial-web

This is the WebXR viewer for the urai-spatial project, built with React, Three.js, and react-three-fiber.

### Running the Development Server

From the root of the `urai-spatial` monorepo, run:

```bash
npm --prefix apps/spatial-web start
```

This will start a local development server and open a browser window to the application.
