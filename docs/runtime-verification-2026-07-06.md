# Runtime verification

Branch head `a7c395e077546df7c6e571d0456b9593b2c7f4aa` passed `tsc -p tsconfig.runtime.json --noEmit` with TypeScript 5.8.3 and Node 22.16.0.

The compiled SystemLoop smoke passed with 3 memory nodes, 3 replay frames, 1 prediction candidate, 1 XR object, persisted run count 1, restored run count 2, and persistence outside the repository.
