# TIER-1 CANON LOCKED — DO NOT MODIFY WITHOUT CANON MIGRATION.

Tier-1 is the immutable URAI spatial foundation.

## Canon meaning
- Tier-1 means the deterministic base spatial loop and home invariant.
- Forward chain is fixed: `HOME -> ASCENT -> LIFEMAP -> FOCUS -> REPLAY`.
- ESC unwind chain is fixed: `REPLAY -> FOCUS -> LIFEMAP -> HOME`.

## Locked standards
- Home invariant: no text, no buttons, no navigation, no onboarding, no narration, sky-primary, spatial-only.
- Official route names: `/`, `/life-map`, `/focus`, `/replay`.
- Canon source export: `urai-tier1/src/canon/tier1.ts`.

## Consumption rules
- App code must import Tier-1 constants from `src/canon/tier1.ts`.
- Do not duplicate phase chains, home invariant definitions, or Tier-1 labels.
