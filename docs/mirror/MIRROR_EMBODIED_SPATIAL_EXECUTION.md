# URAI Mirror — Embodied Spatial Execution Authority

Date: 2026-07-21
Tracking issue: #887
Repository: LifeLoggerAI/urai-spatial
Application root: urai-tier1
Starting main authority: 1279debc452ee846c96bab10cdef4dd9ccaf0cdc
Execution branch: feat/mirror-embodied-spatial-realm-20260721

## Initial truthful verdict

The current public Mirror route is a static promotional page over authored imagery. The active route owner is `urai-tier1/src/app/mirror/page.tsx`; it renders a background image, headline, cards, and ordinary route links. It does not provide a spatial camera, embodied presence, pattern inspection, durable Replay context, or evidence-aware private/demo state handling.

Decision: NO-GO / NOT IMPLEMENTED / NOT MERGED / NOT DEPLOYED.

## Binding product outcome

Mirror must become a calm, inhabitable React Three Fiber reflection chamber integrated with the canonical URAI route and memory systems. It must preserve authorized selected-memory identity, explicitly disclosed demo identity, signed-out and empty-state truth, confidence and provenance, reduced motion, keyboard/touch/pointer access, WebGL recovery, Replay continuity, Passport exit, and 48 CSS-pixel controls.

## Architecture direction

The implementation should reuse:

- `useSelectedMemory` and selected-memory contracts for authorized identity and explicit demo continuity;
- the existing embodied navigation kernel for bounded camera movement, touch controls, drag look, collision, Escape, Enter/Space, and reset;
- React Three Fiber and Three.js already present in `urai-tier1`;
- the canonical world return and route-state systems rather than fixed demo URLs;
- fail-closed privacy and accessibility contracts.

The static image-plus-overlay owner must be removed after the replacement is proven.

## Required evidence

No merge or production claim is authorized until one unchanged exact SHA has:

1. typecheck, unit, contract, build, privacy, accessibility, security, browser, mobile, reduced-motion, and restoration checks passing;
2. retained desktop and portrait-mobile evidence showing real camera movement, embodied reflection, pattern selection, temporal inspection, Replay entry, Passport exit, direct entry, private/demo/empty/signed-out/offline states, clean console and privacy-safe network behavior;
3. all review conversations resolved and any required independent approval recorded;
4. protected merge and production deployment receipts;
5. direct public acceptance at `https://urai.app/mirror`.

Screenshots of a static composition, a local build, a preview URL, or a mergeable PR are not acceptance.
