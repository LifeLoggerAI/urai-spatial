# Live route evidence

Observed canonical markers on `/`, `/ground/`, `/life-map/`, `/focus/`, `/replay/`, `/mirror/`, `/passport/`, and `/ascent/`.

Observed drift:
- `/privacy-controls/` serves Home threshold content instead of the dedicated Privacy Controls page.
- `/status/` still presents the Live Control Room and labels routes live.

No exact deployed commit SHA was visible. Production certification remains blocked.
