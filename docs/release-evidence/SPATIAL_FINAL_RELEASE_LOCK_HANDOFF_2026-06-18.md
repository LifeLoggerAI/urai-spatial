# URAI Spatial Final Release Lock Handoff — 2026-06-18

## Current repo truth

Repository: `LifeLoggerAI/urai-spatial`

Default branch: `main`

Latest confirmed Tier-Five evidence merge: PR #274, `Record Tier Five main post-merge evidence`

Confirmed merge commit: `e1351f153dc2cb73d28e66dfad87b09a7b5f29a9`

Confirmed release chain before this handoff:

- PR #271: Tier Three safe expansion
- PR #272: Tier Four safe expansion
- PR #273: Tier Five safe expansion
- PR #274: Tier Five main post-merge evidence

## Final-product rule

This repo must not be called fully production locked until the current `main` head has fresh proof for all of the following gates on a clean runner or equivalent release machine:

```bash
corepack enable
corepack prepare pnpm@10.0.0 --activate
pnpm install --frozen-lockfile
pnpm lock:static
pnpm lock:build
pnpm test
pnpm xr:verify
pnpm tier5:production:check
pnpm live:check
```

For a single aggregate gate, use:

```bash
pnpm verify:release:full && pnpm tier5:production:check && pnpm live:check
```

## What each gate proves

| Gate | Required proof |
| --- | --- |
| `pnpm lock:static` | Source integrity, runtime boundary, production route exposure, launch copy, tier drift, migration, home invariant, and Firebase rules checks. |
| `pnpm lock:build` | Typecheck and production Next build. |
| `pnpm test` | Functions build/tests, optional browser gate, replay memory theater contract, and replay Tier-Five lock. |
| `pnpm xr:verify` | XR contract, navmesh verification, and Firebase XR preflight without claiming unsupported XR surfaces as live. |
| `pnpm tier5:production:check` | Local Tier-Five production contract remains wired and gated. |
| `pnpm live:check` | Live release guard is satisfied before deployment/recording. |

## Current known external boundary

The GitHub connector can read and write normal repository files, but it cannot execute Firebase deploys, browser runtime checks, or secret/billing-gated live release commands. A direct attempt to add a stronger GitHub Actions workflow from the connector was blocked by platform safety checks because it touched `.github/workflows`.

That means final proof must be generated by one of these two paths:

1. A maintainer/CI runner adds or enables a final release workflow that runs the command matrix above.
2. A release machine with Node twenty-two, pnpm ten, Firebase auth, and browser dependencies runs the command matrix above and records logs under `docs/release-evidence/`.

## Production call

Status as of this handoff: **repo evidence chain is advanced, but full product lock is not claimable until the command matrix above passes on current `main` and live release evidence is recorded.**

Defer any live capability claim that lacks matching current release evidence, explicit consent coverage, and verified production wiring.
