# V50 clean port boundary

Port only deterministic runtime, persistence, smoke, and runtime-contract changes from PR #415 onto current main.

Excluded from the clean port: superseded deployment workflows, alternate deploy scripts, stale Status implementation, and historical audit documents.

## Verification

Code head `a173d18108c460e01d7a19ffa797271662430672` passed the runtime TypeScript check and compiled smoke. The smoke produced 3 memory nodes, 3 replay frames, 1 prediction candidate, 1 XR object, persisted run count 1, restored run count 2, and a durable default state path.
