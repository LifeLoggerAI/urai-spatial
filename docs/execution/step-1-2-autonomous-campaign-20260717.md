# URAI Spatial Step 1 + Step 2 autonomous execution campaign

Date: 2026-07-17

## Authority

- Repository: `LifeLoggerAI/urai-spatial`
- Runtime root: `urai-tier1`
- Phase order: production certification first, world continuity second
- Production mutation is forbidden until exact live authority, rollback, route health, and visual baseline are independently proven.

## Phase 1 — production certification

1. Resolve current `main`, merged convergence authority, production dispatcher, protected deployment run, live fingerprint, Firebase target, and rollback SHA.
2. Inspect every workflow/job/step and retain logs and artifacts.
3. Verify live fingerprint and served asset identity against the certified source.
4. Audit `/`, `/home`, `/ground`, `/life-map`, `/focus`, `/replay`, `/mirror`, `/passport`, `/privacy-controls`, `/location-map`, `/status`, and `/spatial/ar-vr`.
5. Capture mobile, tablet, and desktop screenshots plus full Home → Ground → Life Map → Focus → Replay → return recordings.
6. Record console, network, hydration, missing-asset, keyboard, reduced-motion, clipping, overflow, and route-history defects.
7. Search live and source output for demo, fallback, placeholder, developer, and environment-warning residue.
8. Produce an exact-SHA certification verdict and rollback receipt.

## Current independently observed production defect

Direct live inspection on 2026-07-17 shows `/home` still exposes user-facing prototype/fallback copy including:

- `Source: demo / focused`
- `Build with us`
- `Help us tune the Life Movie.`
- `We ship daily`
- `Feedback capture is paused because Firebase isn’t configured in this environment.`
- `Bug intake is paused here.`

This is a release-blocking presentation defect for the final product and must enter the Phase 2 defect matrix. It also indicates that merged source and visually certified production must not be treated as equivalent without fingerprint and artifact proof.

## Phase 2 — continuous spatial world

1. Lock continuity contracts for persistent memory identity, orb, atmosphere, audio, camera, navigation history, narrator state, motion settings, and escape stack.
2. Finish or consolidate one persistent world shell shared by Home, Ground, Life Map, Focus, and Replay.
3. Make Home an inhabited threshold rather than a landing page.
4. Make Ground a physical destination layer rather than a route rail or dashboard.
5. Make Life Map a full-screen navigable memory cosmos.
6. Make Focus a memory chamber retaining exact selected-memory identity.
7. Make Replay a cinematic transformation of that same chamber and return deterministically.
8. Replace webpage residue with environmental objects, spatial cues, contextual overlays, and progressive disclosure.
9. Validate full motion, reduced motion, mobile safe areas, keyboard, browser back, Escape unwind, performance, and accessibility.
10. Require exact-head screenshots and recordings for every candidate.

## Completion gate

The campaign is complete only when production serves the exact certified SHA, the fingerprint matches, rollback is proven, the full journey works across required viewports, memory identity persists, demo/developer residue is absent, visual inspection passes, and all retained evidence is checksummed and mapped to immutable GitHub and deployment authority.
