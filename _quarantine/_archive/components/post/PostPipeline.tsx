'use client'

import { EffectComposer } from '@react-three/postprocessing'

/**
 * Minimal post-processing pipeline.
 * Keep this empty first, then add only verified effects.
 */

export default function PostPipeline() {
  return <EffectComposer />
}