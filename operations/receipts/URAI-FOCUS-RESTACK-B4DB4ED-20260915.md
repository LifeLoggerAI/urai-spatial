# URAI Focus Restack Receipt — current Spatial successor

Date: 2026-09-15
Repository: `LifeLoggerAI/urai-spatial`
Base branch: `home-final-v24-certify-20260826`
Current reconciled base SHA: `b065f6ea64e4e3aa33711b8d8c6d7a8c8f52809c`
Restack branch: `focus-final-restack-b4db4ed-20260915`

## Reason

PR #1177 advanced through predecessor Spatial heads and most recently moved from `b4db4ed6224eb0e392205c78657050c5d288b8f0` to `b065f6ea64e4e3aa33711b8d8c6d7a8c8f52809c` to reconcile a Home first-person/no-avatar contract test. That latest base move changed one Home test file and no Focus or Life Map runtime file. The Focus branch explicitly reconciled the exact `b065f6ea...` change before earning new evidence.

All workflow, pixel, review, approval and governance evidence remains exact-head bound and does not transfer across source commits.

## Portable Focus delta carried forward

- canonical `/focus` metadata;
- compatibility fallback copy cleanup;
- Related Context semantics without renaming Focus state IDs;
- Focus canonical terminology lock;
- Architecture Lock Focus terminology extension;
- Focus terminology regression contracts;
- Focus-specific Gold Master proof harness;
- exact-head Focus Gold Master workflow;
- visible/current Focus and Replay shell selection in proof automation;
- committed-navigation journey waits that preserve strict route assertions without requiring an unrelated full-load lifecycle;
- canonical explicit-demo narration.

## Literal-pixel repair

A retained predecessor Focus artifact was opened at full resolution and showed a real visual defect: stale static `--focus-asset` artwork containing the rejected glowing sphere / ring-cage presentation was visually dominating the screen while the newer live WebGL runtime was suppressed to partial opacity.

The restack repairs the source rather than the proof threshold:

- static Focus backdrop is retired from visible authority;
- live WebGL Focus returns to full-opacity, normal-blend rendering;
- the newer fractured, grounded selected-memory manifestation remains the live scene owner;
- rejected tunnel-ring, cradle and repeated-rune GLB nodes remain retired by runtime source;
- regression coverage now fails closed if the stale static artwork becomes visible authority again.

## Preservation boundary

The newer #1177 visual/runtime implementation remains authoritative. The isolated restack does not import old-main Focus visuals or replace the newer geometry, lighting, camera, material, movement, asset, privacy, or state logic.

The Focus proof by itself does not establish Life Map -> Focus approach acceptance, independent approval, governance, deployment, or live parity. Literal acceptance requires opening current-head retained images.

## Public copy reconciliation completed

Public Focus chamber-era wording was reconciled to canonical Focus language while stable internal chamber/aperture identifiers remain. This includes canonical `Enter Replay` accessible copy, Focus loading/fallback/status wording, Focus controls, neutral Focus Observatory language, and explicit demo narration. The production-route verifier requires the canonical `Enter Replay` accessible action.

## Exact-head rule

Any later base or restack-head move invalidates evidence earned for the predecessor SHA. GitHub live metadata is authoritative.
