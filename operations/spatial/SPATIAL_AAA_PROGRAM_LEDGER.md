# UrAi Spatial AAA Program Ledger

Status: active stacked execution authority during registrar/DNS hold  
Repository: `LifeLoggerAI/urai-spatial`  
Stacked base: `1826d0eacba7eab98f9e844492696dff289028a7`  
Program branch: `reconcile/ecosystem-authority-after-geography-20260801`  
Production authority: issue `#999`

## Objective

Converge UrAi onto one coherent, persistent, embodied spatial world while preserving privacy, accessibility, safe fallbacks, exact-head evidence, and the separation between preview and protected production. DNS blocks canonical-domain cutover; it does not excuse repository defects or stale authority.

## Exact dependency authority

1. Trusted preview PR #1018 — repaired exact head `5dd029aea3e63fa307a4c9b8be6122ba65a39ab8`.
2. Consent-gated geography PR #1013 — `1826d0eacba7eab98f9e844492696dff289028a7`, stacked behind #1018.
3. Ecosystem governance PR #1019 — this branch, stacked behind #1013.
4. Protected production — final exact `main` only, governed by issue #999.

No workflow or artifact from an earlier head transfers merge authority to a newer head.

## Frozen boundaries

- `brand/BRAND_AUTHORITY.md` and governed design tokens remain authoritative.
- Certified V1 asset lineage is preserved; later assets require separate admission evidence.
- Symbolic Life Map remains primary; geography is explicit-action supporting context.
- Provider service enablement does not equal restricted credential or product-use authorization.
- Trusted preview must remain visibly non-production and isolated from channel `live`.
- Legal, consent, settings, and dense reading surfaces may remain accessible world-native panels rather than forced 3D scenes.
- Unsupported medical, HIPAA, anonymity, encryption, patent, partnership, earnings, and universal-accessibility claims remain prohibited.

## Shared integration contract

Every embodied realm must converge on the same:

1. canvas and route ownership;
2. persistent world and player state;
3. camera lifecycle and transition language;
4. realm lifecycle and disposal;
5. world-coordinate and portal registry;
6. Orb continuity where canon permits it;
7. lighting, atmosphere, color management, and quality tiers;
8. asset preload, cache, reference counting, and disposal;
9. keyboard, pointer, touch, reduced-motion, and fallback inputs;
10. spatial audio and haptic equivalents;
11. no-WebGL and low-power fallback;
12. exact-build diagnostics and preview identity;
13. consent, privacy, export, deletion, and cross-user denial boundaries.

An isolated 3D route that cannot satisfy this contract is not accepted as a spatial realm.

## Current execution waves

### Wave A — launch-critical dependency closure

- A1: finish and inspect PR #1018 repaired exact-head matrix and visual proof;
- A2: merge #1018 only if exact head `5dd029ae…` remains unchanged and accepted;
- A3: run and inspect the isolated post-merge Firebase preview channel;
- A4: retarget #1013 to resulting `main`, run the replacement matrix, inspect cloud/privacy/visual evidence;
- A5: retarget #1019 to resulting `main`, run the replacement matrix, inspect governance and native doorway evidence.

### Wave B — external production authority

- complete Squarespace identity/account recovery;
- renew all six domains and retain redacted receipts/new expiration dates;
- restore nameservers, DNS, TLS, and `urai.app` fingerprint reachability;
- re-resolve exact current `main` and rollback authority;
- dispatch one protected production run;
- retain route, desktop, mobile, accessibility, privacy, resource, console, network, fingerprint, and rollback evidence.

### Wave C — post-launch estate certification

- Admin, Privacy, Jobs, and Staging protected-cloud lifecycle proof;
- Communications delivery pilot and monitoring;
- Analytics, Content, and Studio deployment integration;
- Storytime, B2B, and Investors internal-alpha closure;
- Foundation, Labs, and Marketing legal/DNS/public-authority proof;
- permanent legacy credential quarantine and provider-side revocation proof.

## Superseded failure reconciliation

Former ecosystem head `9dcf54008ea10cb40af06cfe0afcc74069acf815` failed Native Doorway proof only for desktop semantic-pointer Home → Ground while keyboard, mobile touch, and all tested Life Map paths passed.

Former preview head `00caa02db5dac79e1334a959ce4bd6b4f7d79d7b` completed all captured visual and interaction assertions without console, page, or request errors, but its first portal-sequence screenshot exceeded Playwright's default hosted-runner timeout. The repaired head `5dd029ae…` isolates portal and fallback phases in fresh Chromium processes and applies an explicit screenshot allowance without weakening any product assertion. It must obtain its own complete replacement evidence.

## Acceptance rules

A realm or governance lane is complete only when its exact tested SHA has retained proof appropriate to its scope. Spatial surfaces require desktop/mobile interaction, departure/arrival, navigation restoration, loading, empty/partial/error states, reduced motion, safe tier, no-WebGL fallback, keyboard equivalence, accessible semantics, console/request diagnostics, and exact build identity.

Governance surfaces require machine-verifiable default-deny controls, truthful dependency SHAs, prohibited-claim enforcement, external-authority boundaries, and no production/provider/legal mutation.

Claims such as `AAA+++`, `production ready`, `fully accessible`, `HIPAA compliant`, `patent pending`, `partnered`, or `complete` remain prohibited unless retained scope-specific evidence supports them.

## Merge discipline

- merge in dependency order only;
- use expected-head protection;
- do not transfer stale evidence;
- do not force-push or weaken tests;
- do not use an unknown reviewer as release authority;
- record resulting `main` SHA, workflows, artifacts, limitations, rollback, cost, and mutation state after each accepted merge.

## Cost boundary

Existing governed assets are preferred. Any paid provider use requires an explicit bounded request count, cost ceiling, provider/model receipt, request identifiers where available, provenance, and no unbounded retry behavior.

## External blocker boundary

DNS may block canonical-domain cutover and certificate verification only. Stale branches, merge conflicts, failed checks, missing screenshots, inaccurate provider state, and performance regressions are repository repair work—not external blockers.
