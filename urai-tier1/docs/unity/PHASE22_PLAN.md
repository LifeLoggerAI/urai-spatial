# Phase 22: Unity Adapter Consuming Export Manifest

Goal:
Create the first Unity-facing adapter contract from the locked export, XR, and AR layers.

Targets:
- derive Unity adapter mode from current scene state
- expose a compact export manifest for downstream Unity consumers
- preserve build/export/deploy green path
