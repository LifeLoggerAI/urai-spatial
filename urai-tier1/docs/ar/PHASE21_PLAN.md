# Phase 21: AR Placement Mode

Goal:
Define a first AR placement contract on top of the XR runtime and export layers.

Targets:
- derive placement mode from current scene/export/xr state
- expose placement hints without breaking the locked web baseline
- preserve build/export/deploy green path
