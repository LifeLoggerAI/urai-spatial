# Phase 26: External JSON/CSV Ingest and Canonical Validation

Goal:
Start real external ingest by parsing JSON and CSV rows into canonical memory contracts.

Targets:
- parse JSON and CSV rows
- normalize external rows into canonical memory shape
- expose ingest readiness and errors
- preserve build/export/deploy green path
