# Phase 25: Real Data Import and Normalization Pipeline

Goal:
Start the next tier by measuring import readiness and normalization quality for larger real memory datasets.

Targets:
- compute dataset coverage across chapters, timebands, and emotions
- expose import readiness and gaps on top of the locked spatial shell
- preserve build/export/deploy green path
