# Phase 19: WebXR Scene Entry

Goal:
Create a first real WebXR scene-entry layer on top of the locked export manifest and immersive contracts.

Targets:
- detect browser WebXR support client-side
- derive target XR mode from current scene export state
- expose enter/exit XR controls without destabilizing static export
- preserve build/export/deploy green path
