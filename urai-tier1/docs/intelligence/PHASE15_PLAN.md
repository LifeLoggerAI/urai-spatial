# Phase 15: LifeMap Intelligence and Pattern Synthesis

Goal:
Add the first pattern-synthesis layer on top of the locked Tier 1 baseline.

Targets:
- derive pattern score from memory, cluster, and narrative contracts
- show synthesis only when a memory is focused outside replay
- preserve replay, clustering, and memory sphere behavior
- keep export and hosting green
