# Phase 34: Era Compare Mode

Goal:
Compare the selected memory against a nearby era match to expose continuity and change.

Targets:
- derive nearest era comparison target from dataset signals
- expose similarities, differences, and compare basis
- preserve build/export/deploy green path
