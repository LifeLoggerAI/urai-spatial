# Phase 23: Controller and Gesture Mapping

Goal:
Define the first XR controller and gesture contract on top of the current XR, AR, and Unity adapter stack.

Targets:
- derive input mode from current immersive state
- expose select, confirm, scale, rotate, and replay/focus channels
- preserve build/export/deploy green path
