{
  "name": "urai-tier1",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --webpack",
    "build": "next build --webpack",
    "start": "next start",
    "lint": "tsc --noEmit -p tsconfig.runtime.json",
    "typecheck": "tsc --noEmit -p tsconfig.runtime.json",
    "typecheck:legacy": "tsc --noEmit -p tsconfig.legacy.json",
    "verify:tier-lock": "node scripts/verify-tier-lock.mjs",
    "audit:visual-lock": "npm run verify:tier-lock && npm run typecheck && npm run build",
    "audit:final-visual": "node scripts/audit-final-visual.mjs",
    "audit:final": "npm run audit:final-visual && npm run audit:visual-lock",
    "test": "node --experimental-strip-types --test tests/*.test.mjs"
  },
  "dependencies": {
    "@react-three/fiber": "^9.5.0",
    "@react-three/postprocessing": "^3.0.4",
    "@tailwindcss/postcss": "^4.2.2",
    "firebase": "^12.11.0",
    "next": "^16.1.6",
    "react": "19.2.4",
    "react-dom": "^19.2.4",
    "three": "^0.183.2",
    "three-stdlib": "^2.36.1"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@types/three": "^0.183.1",
    "playwright": "^1.57.0",
    "typescript": "^6.0.3"
  }
}
