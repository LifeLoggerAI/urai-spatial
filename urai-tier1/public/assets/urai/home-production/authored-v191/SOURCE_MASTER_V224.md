# Home V224 direct-runtime visual authority

V224 exists because exact-head `72325d5de367f8768a071ab4ed876642e71b002d` passed its Portal/Orb workflow but failed literal retained-pixel inspection: the visible Home still read as a broad heightfield with dark block-like destinations and a twisted-shard Orb.

The current visible production candidate is therefore rendered directly by `src/spatial/layout/HomeWorldProductionV223Geometry.tsx`. The historical V191 GLBs remain in the repository for provenance and transport integrity only; they are **not mounted as the V224 visible world** and confer no visual acceptance.

The direct V224 topology deliberately replaces the rejected visible families:

- a high-density sculpted movement floor is retained for continuity, while visible depth comes from eight overhanging non-heightfield cliff sheets, rooted memory ribs, and distant buttresses;
- Ground is an open sheltering place with a curved rooted wall, grown-in approach path, and embedded warm hearth;
- Life Map is an open sculptural observatory with an asymmetric three-fold memory ledger, rising path, and four embedded lineage traces;
- the Orb is one closed, smooth, asymmetric living-memory surface with paired crown, lower taper, localized recessed dimple, tactile striation, authored color field, breathing and state posture. It is not a tube/S-curve, particle shell, ring, mineral cluster, or three-ball form.

`generate-home-authored-v224-assets.py` is retained as a deterministic binary-authoring source for future governed asset materialization. The current connected GitHub ref-update path does not trigger branch-push authoring workflows, so those generated binaries are not represented as current mounted runtime authority.

Fresh exact-head browser capture and literal inspection remain mandatory. This document is source provenance, not visual approval.
