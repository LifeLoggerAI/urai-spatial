# Phase 4 Intent

Goal:
Turn replay into a visible canonical memory-dive scene instead of only a mode toggle.

Expected outcome:
- Home -> LifeMap -> Focus -> Replay path remains intact
- replay has its own overlay presentation
- camera intent differs by mode
- selected star gets memory sphere / replay constellation treatment
- ESC exits replay -> focus, focus -> lifemap, lifemap -> home
