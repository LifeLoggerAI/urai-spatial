'use client'

import { HomeOrbGroundedV288 } from '../assets/HomeOrbGroundedV288'

/**
 * V288 literal-pixel authority.
 *
 * The earlier sanctuary-dressing layer injected primitive cylinders,
 * icosahedrons, cairns, lanterns and fireflies into the current Home. Those
 * shapes are historical art provenance, not Gold-Master pixels. Current Home
 * authority is deliberately narrow: the physical-world terrain is owned by
 * HomeV225PolishV3 and the living companion pixels are owned here by V288.
 */
export function HomeVisualAuthority() {
  return <HomeOrbGroundedV288 />
}
