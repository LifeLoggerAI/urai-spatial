import type { ReactNode } from 'react'
import './aaa-mobile.css'

export default function ReplayFilmLayout({ children }: { children: ReactNode }) {
  return <div className="urai-replay-film-route">{children}</div>
}
