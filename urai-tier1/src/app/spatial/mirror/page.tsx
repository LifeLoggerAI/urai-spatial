import MirrorRealWorldRealm from '@/spatial/realms/MirrorRealWorldRealm'

export const metadata = {
  title: 'URAI Mirror Chamber',
  description: 'Enter the physical Mirror chamber before opening the governed reflection layer.',
}

export default function SpatialMirrorPage() {
  return <MirrorRealWorldRealm />
}
