export default function Loading() {
  return (
    <main className="tier-one-boundary" aria-live="polite" aria-busy="true">
      <section className="tier-one-boundary__card">
        <p className="tier-one-boundary__eyebrow">URAI Spatial</p>
        <h1>Opening the spatial field...</h1>
        <p>The scene is loading. If this takes more than a few seconds, refresh once or check your connection.</p>
      </section>
    </main>
  )
}
