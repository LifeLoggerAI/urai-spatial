# urai-spatial Cloud Functions

This directory contains the Cloud Functions for the `urai-spatial` project.

## Local Emulation

To emulate the functions locally, run the following command from the root of the project:

```bash
firebase emulators:start
```

## Deployment

To deploy the functions to your Firebase project, run:

```bash
firebase deploy --only functions
```
