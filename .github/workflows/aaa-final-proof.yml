name: URAI AAA Final Proof

on:
  workflow_call:
    inputs:
      target_sha:
        description: Exact source SHA to verify
        required: true
        type: string
      base_url:
        description: Base URL to smoke after build
        required: false
        type: string
        default: http://