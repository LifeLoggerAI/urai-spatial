name: Focus Gold Master Proof

on:
  workflow_dispatch:
    inputs:
      verification_sha:
        description: Exact 40-character commit SHA to verify
        required: true
        type: string
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/focus-gold-master-proof.yml'
      - 'scripts/capture-focus-gold-master-proof.mjs'
      - 'scripts/run-with-ci-webgl.mjs'
      - 'urai-tier1/src/app/focus/**'
      - 'urai-tier1/src/app/replay/**'
      - 'urai-tier1/src/spatial/memory/**'
      - 'urai-tier1/src/spatial/world/**'
      - 'urai-tier1/src/spatial/performance/**'

permissions:
  contents: read

concurrency:
  group: focus-gold-master-proof-${{ github.event.pull_request.number || inputs.verification_sha || github.ref }}
  cancel-in-progress: true

env:
  URAI_EXACT_HEAD: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.event_name == 'workflow_dispatch' && inputs.verification_sha || github.sha }}
  PLAYWRIGHT_BROWSERS_PATH: '0'
  NEXT_PUBLIC_URAI_EXPLICIT_DEMO: 'true'

jobs:
  exact-head-focus-proof:
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: ${{ env.URAI_EXACT_HEAD }}
          fetch-depth: 1
          persist-credentials: false
          show-progress: false

      - name: Prove exact clean candidate
        shell: bash
        run: |
          set -euo pipefail
          [[ "$URAI_EXACT_HEAD" =~ ^[0-9a-f]{40}$ ]]
          test "$(git rev-parse HEAD)" = "$URAI_EXACT_HEAD"
          test -z "$(git status --porcelain --untracked-files=all)"

      - name: Setup pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1
        with:
          version: 10.0.0
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install exact lockfile
        run: |
          corepack enable
          pnpm install --frozen-lockfile

      - name: Build exact static release candidate
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/focus-gold-master-proof
          pnpm build:static 2>&1 | tee artifacts/focus-gold-master-proof/build-static.log

      - name: Install Chromium and validate proof source
        shell: bash
        run: |
          set -euo pipefail
          pnpm --dir urai-tier1 exec playwright install --with-deps chromium
          pnpm --dir urai-tier1 exec playwright --version
          node --check scripts/run-with-ci-webgl.mjs
          node --check scripts/capture-focus-gold-master-proof.mjs

      - name: Capture exact-head Focus proof
        shell: bash
        run: |
          set -euo pipefail
          output=artifacts/focus-gold-master-proof
          python3 -m http.server 4173 --bind 127.0.0.1 --directory urai-tier1/out > "$output/http-server.log" 2>&1 &
          server_pid=$!
          trap 'kill "$server_pid" 2>/dev/null || true' EXIT
          for attempt in $(seq 1 60); do
            curl --fail --silent http://127.0.0.1:4173/focus/ >/dev/null && break
            sleep 1
          done
          curl --fail --silent --show-error http://127.0.0.1:4173/focus/ >/dev/null
          URAI_PROOF_BASE=http://127.0.0.1:4173 \
          URAI_PROOF_DIR="$output" \
          URAI_EXACT_HEAD="$URAI_EXACT_HEAD" \
            node scripts/run-with-ci-webgl.mjs scripts/capture-focus-gold-master-proof.mjs 2>&1 | tee "$output/capture.log"

      - name: Prove complete exact-head receipt
        shell: bash
        run: |
          set -euo pipefail
          node --input-type=module - artifacts/focus-gold-master-proof/receipt.json "$URAI_EXACT_HEAD" <<'NODE'
          import { readFile } from 'node:fs/promises'
          const [receiptPath, head] = process.argv.slice(2)
          const receipt = JSON.parse(await readFile(receiptPath, 'utf8'))
          if (receipt.exactHead !== head) throw new Error('Focus proof exact-head mismatch')
          if (receipt.captures.length !== 10) throw new Error(`Expected 10 Focus captures, found ${receipt.captures.length}`)
          if (!receipt.journey?.passed || receipt.journey?.steps?.length !== 5) throw new Error('Focus continuity journey is incomplete')
          if (receipt.errors.length) throw new Error(`Focus proof receipt contains ${receipt.errors.length} errors`)
          NODE

      - name: Upload exact-head Focus Gold Master candidate proof
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: focus-gold-master-proof-${{ env.URAI_EXACT_HEAD }}
          path: artifacts/focus-gold-master-proof
          if-no-files-found: error
          retention-days: 90
          compression-level: 6
