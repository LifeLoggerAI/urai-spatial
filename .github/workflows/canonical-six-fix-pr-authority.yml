name: Canonical six-fix command authority

on:
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: read

concurrency:
  group: canonical-six-fix-command-authority
  cancel-in-progress: false

jobs:
  repair:
    if: github.event.issue.number == 788 && github.event.issue.pull_request &&