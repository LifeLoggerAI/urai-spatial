name: Dispatch URAI Production Once
# Protected convergence for the fingerprint-gated Status smoke repair; workflow runs are listed broadly and exact-SHA filtered locally to avoid unstable server-side head_sha queries.
# Retriggered after merged Home sanctuary convergence on 2026-07-20; no release logic changed.

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/dispatch-production-once.yml'

permissions:
  actions: write
  contents: read
  issues: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Detect live authority and dispatch the correct protected release mode
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const