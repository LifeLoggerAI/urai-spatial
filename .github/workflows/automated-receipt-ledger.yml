name: URAI Automated Receipt Ledger

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    - cron: '17 9 * * *'
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  pull-requests: read

concurrency:
  group: urai-receipt-ledger-${{ github.ref }}
  cancel-in-progress: false

jobs:
  receipt-ledger:
