name: Investor Materials Control

on:
  pull_request:
    paths:
      - '.github/workflows/investor-materials-control.yml'
      - 'docs/founder-readiness/INVESTOR_*.md'
      - 'release/investor-materials-state.json'
      - 'scripts/verify-investor-materials-control.mjs'
  push:
    branches: [main]
    paths:
      - '.github/workflows/investor-materials-control.yml'
      - 'docs/founder-readiness/INVESTOR_*.md'
      - 'release/investor-materials-state.json'
      - 'scripts/verify-investor-materials-control.mjs'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: investor-materials-control-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  TARGET_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_SHA }}
          fetch-depth: 0
          persist-credentials: false
      - name: Prove exact clean head
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$TARGET_SHA"
          test -z "$(git status --porcelain)"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Verify investor materials control
        run: |
          node --check scripts/verify-investor-materials-control.mjs
          node scripts/verify-investor-materials-control.mjs
